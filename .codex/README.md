# C:\PDJ Codex Notes

Purpose: private BarMatrix content rebuild workspace.

Target route prefix: `/Jesuslovesyou/`.

Reference roots:

- `C:\PDJ\incoming` - source documents for the rebuild.
- `C:\BMO` - read-only ops/reference project.
- `C:\barmatrix-app` - read-only current frontend reference until implementation scope is explicit.
- `C:\barmatrix-api` - read-only current backend reference until implementation scope is explicit.

Use `tasks/todo.md` as the run ledger.
